// offworld :: menu :: fabric — published leaf (cosmetic; no cdylib).
plugins { base }

base { archivesName.set("offworld-menu-fabric") }

// dependencies { implementation(project(":offworld:menu")) }
// TODO(you): apply the Fabric loader plugin + fabric.mod.json.
