// offworld :: tech (common) — gameplay/worldgen logic, loader-agnostic.
// The genesis consumer; depends on content. See docs/genesis-rust.md §4.
plugins { base }

base { archivesName.set("offworld-tech-common") }

// dependencies { implementation(project(":offworld:content")) }
// TODO(you): java/kotlin common gameplay/worldgen logic.
