// offworld :: menu (common) — start-screen reskin logic, loader-agnostic.
// Touches neither genesis nor the cdylib. See docs/genesis-rust.md §5.
plugins { base }

base { archivesName.set("offworld-menu-common") }

// TODO(you): java/kotlin common start-screen logic.
