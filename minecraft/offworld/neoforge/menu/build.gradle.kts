// offworld :: menu :: neoforge — published leaf (cosmetic; no cdylib).
plugins { base }

base { archivesName.set("offworld-menu-neoforge") }

// dependencies { implementation(project(":offworld:menu")) }
// TODO(you): apply the NeoForge loader plugin + neoforge.mods.toml.
