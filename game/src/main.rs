//! game — the native standalone host. Links genesis_core directly; pays no
//! boundary tax. See docs/genesis-rust.md §2, §4, and Phase 5b.

fn main() {
    println!("offworld — native host (genesis not wired yet)");
}
