[workspace]
resolver = "3"
members = [
    "genesis/core",
    "genesis/ffi",
    "genesis/viz",
    "game",
]

[workspace.package]
edition = "2024"
version = "0.0.0"
rust-version = "1.85"

[workspace.dependencies]
genesis-core = { path = "genesis/core" }
genesis-viz = { path = "genesis/viz" }
